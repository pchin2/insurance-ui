{
  "name": "insurance-ui",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Single source of truth for shared UI across Phillip Chin's four insurance sites (TBI, GLI, PWP, LIH).",
  "scripts": {
    "build": "node build.mjs",
    "release": "bash scripts/release.sh",
    "rollback": "bash scripts/rollback.sh"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
